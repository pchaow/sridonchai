{
  "name": "sridonchai",
  "version": "1.0.0",
  "description": "sridonchai water management",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd client && yarn install",
    "dev": "cd client && yarn dev",
    "build": "cd client && yarn build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@tasoskakour/react-use-oauth2": "^2.0.0"
  }
}
